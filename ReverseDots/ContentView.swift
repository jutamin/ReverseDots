//
//  ContentView.swift
//  ReverseDots
//
//  Created by 원주연 on 3/31/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, ReverseDots!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
